version: 1.0
runtime: python3
run:
  command: python3 -m pip install -r requirements.txt && python3 -m streamlit run main.py --server.port 8080 --server.address 0.0.0.0
  network:
    port: 8080
    
