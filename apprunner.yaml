version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt --target /app/vendor
      - chmod +x /app/start.sh
run:
  command: /bin/sh /app/start.sh
  network:
    port: 8080
  env:
    - name: PYTHONPATH
      value: "/app/vendor:/app"
